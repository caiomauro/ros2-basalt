Baseline captured on 2026-04-15 with:

- world: `default_vio`
- model: `x500_stereo_cam`
- mission: a local square-waypoint mission
- cameras: `20 Hz`
- IMU: `200 Hz`
- comparer: `scripts/compare_basalt_truth.py`

Observed baseline from user-provided comparer output:

- initial offset:
  - `delta_xyz ~= (-0.010, 0.002, 0.319) m`
- raw axis ranges over run:
  - `x: [-0.298, 0.005] m`
  - `y: [-0.200, 0.026] m`
  - `z: [0.228, 0.377] m`
- with initial `z` offset removed (`z - 0.319`):
  - adjusted `z: [-0.091, 0.058] m`
- overall error:
  - `rmse ~= 0.337 m`
  - `max ~= 0.454 m`

Intent of later tuning:

- reduce startup origin bias, especially in `z`
- preserve the stable x500 behavior restored after reverting from VTOL defaults
